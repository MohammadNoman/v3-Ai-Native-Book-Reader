<div className="space-y-10">

{/* Section 6.1 */}
<div>
  <h2 className="text-4xl font-bold text-white mb-6 flex items-center gap-3">
    <span className="text-4xl">🎨</span>
    6.1 The Interface Challenge
  </h2>
  
  <div className="bg-gradient-to-br from-purple-950 via-slate-900 to-blue-950 rounded-2xl p-8 border border-purple-800/30 shadow-xl">
    <p className="text-xl text-slate-200 leading-relaxed mb-4">
      We now have a brain, but it needs a <strong className="text-cyan-400">body</strong>.
    </p>
    
    <div className="bg-slate-900/50 rounded-lg p-6 border border-slate-700">
      <p className="text-lg text-white font-bold mb-2">The Solution:</p>
      <p className="text-slate-300">
        Build a <strong className="text-purple-400">'ChatKit' compatible widget</strong> that floats on our documentation site, handling state, history, and connection to our FastAPI backend.
      </p>
    </div>
  </div>
</div>

{/* Section 6.2 - React Widget */}
<div>
  <h2 className="text-4xl font-bold text-white mb-6 flex items-center gap-3">
    <span className="text-4xl">⚛️</span>
    6.2 The React Widget Component
  </h2>
  
  <p className="text-lg text-slate-300 mb-6 leading-relaxed">
    This component uses React hooks to manage the chat lifecycle. It also listens for text selection events for <strong className="text-cyan-400">Context-Aware querying</strong>.
  </p>
  
  <div className="grid md:grid-cols-3 gap-4 mb-6">
    <div className="bg-gradient-to-br from-blue-500/10 to-cyan-500/10 rounded-xl p-6 border border-blue-500/30">
      <div className="text-3xl mb-3">💬</div>
      <h4 className="text-xl font-bold text-white mb-2">State Management</h4>
      <p className="text-slate-300 text-sm">React hooks for messages & input</p>
    </div>
    
    <div className="bg-gradient-to-br from-purple-500/10 to-pink-500/10 rounded-xl p-6 border border-purple-500/30">
      <div className="text-3xl mb-3">🎯</div>
      <h4 className="text-xl font-bold text-white mb-2">Text Selection</h4>
      <p className="text-slate-300 text-sm">Captures highlighted context</p>
    </div>
    
    <div className="bg-gradient-to-br from-green-500/10 to-emerald-500/10 rounded-xl p-6 border border-green-500/30">
      <div className="text-3xl mb-3">🔌</div>
      <h4 className="text-xl font-bold text-white mb-2">API Integration</h4>
      <p className="text-slate-300 text-sm">Connects to FastAPI backend</p>
    </div>
  </div>
  
  <div className="bg-slate-900 rounded-2xl p-6 border border-slate-700 shadow-xl">
    <div className="flex items-center gap-3 mb-4">
      <span className="text-2xl">💻</span>
      <h4 className="text-lg font-bold text-cyan-400">src/components/ChatWidget.js</h4>
    </div>
    
    <div className="bg-slate-950 rounded-lg p-6 border border-slate-800 font-mono text-xs overflow-x-auto">
      <pre className="text-slate-300">{`export const ChatWidget = () => {
  const [isOpen, setIsOpen] = useState(false);
  const [messages, setMessages] = useState([]);
  const [selection, setSelection] = useState('');

  // Listen for text selection
  useEffect(() => {
    const handleSelection = () => {
      const text = window.getSelection().toString();
      if (text.length > 5) setSelection(text);
    };
    document.addEventListener('mouseup', handleSelection);
    return () => document.removeEventListener('mouseup', handleSelection);
  }, []);

  const sendMessage = async () => {
    const res = await fetch('http://localhost:8000/ask', {
      method: 'POST',
      body: JSON.stringify({
        message: input,
        selected_context: selection
      })
    });
    const data = await res.json();
    setMessages(prev => [...prev, { role: 'model', text: data.reply }]);
  };
  
  return (/* Chat UI */)
};`}</pre>
    </div>
  </div>
</div>

{/* Section 6.3 - Docusaurus Integration */}
<div>
  <h2 className="text-4xl font-bold text-white mb-6 flex items-center gap-3">
    <span className="text-4xl">🔧</span>
    6.3 Docusaurus Integration (Swizzling)
  </h2>
  
  <p className="text-lg text-slate-300 mb-6 leading-relaxed">
    To make this widget appear on every page, we wrap the root application.
  </p>
  
  <div className="bg-gradient-to-br from-cyan-500/10 to-blue-500/10 rounded-xl p-6 border border-cyan-500/30 mb-6">
    <div className="space-y-3">
      <div className="flex items-start gap-3">
        <div className="w-8 h-8 bg-cyan-500 rounded-full flex items-center justify-center text-white font-bold flex-shrink-0">1</div>
        <div>
          <p className="font-bold text-white">Run swizzle command</p>
          <code className="text-cyan-400 text-sm">npm run swizzle @docusaurus/theme-classic Root -- --wrap</code>
        </div>
      </div>
      
      <div className="flex items-start gap-3">
        <div className="w-8 h-8 bg-blue-500 rounded-full flex items-center justify-center text-white font-bold flex-shrink-0">2</div>
        <div>
          <p className="font-bold text-white">Edit src/theme/Root.js</p>
          <p className="text-slate-400 text-sm">Add ChatWidget to the root component</p>
        </div>
      </div>
    </div>
  </div>
  
  <div className="bg-slate-900 rounded-2xl p-6 border border-slate-700 shadow-xl">
    <div className="flex items-center gap-3 mb-4">
      <span className="text-2xl">💻</span>
      <h4 className="text-lg font-bold text-cyan-400">src/theme/Root.js</h4>
    </div>
    
    <div className="bg-slate-950 rounded-lg p-6 border border-slate-800 font-mono text-xs overflow-x-auto">
      <pre className="text-slate-300">{`import React from 'react';
import { ChatWidget } from '../components/ChatWidget';

export default function Root({children}) {
  return (
    <>
      {children}
      <ChatWidget />
    </>
  );
}`}</pre>
    </div>
  </div>
</div>

{/* Section 6.4 - The Result */}
<div>
  <h2 className="text-4xl font-bold text-white mb-6 flex items-center gap-3">
    <span className="text-4xl">✨</span>
    6.4 The Result
  </h2>
  
  <div className="bg-gradient-to-r from-green-500/10 to-emerald-500/10 rounded-2xl p-8 border border-green-500/30">
    <div className="text-center mb-6">
      <div className="text-5xl mb-4">🎉</div>
      <h3 className="text-2xl font-bold text-white mb-4">A Documentation Site That Listens</h3>
    </div>
    
    <div className="bg-slate-900/50 rounded-lg p-6 border border-slate-700">
      <p className="text-lg text-slate-200 leading-relaxed mb-4">
        Users can now:
      </p>
      
      <div className="space-y-3">
        <div className="flex items-start gap-3">
          <span className="text-green-400 text-xl">✓</span>
          <p className="text-slate-300">Highlight a confusing paragraph about 'Authentication'</p>
        </div>
        
        <div className="flex items-start gap-3">
          <span className="text-green-400 text-xl">✓</span>
          <p className="text-slate-300">Click the chat bubble</p>
        </div>
        
        <div className="flex items-start gap-3">
          <span className="text-green-400 text-xl">✓</span>
          <p className="text-slate-300">Get an AI explanation using the exact context plus semantic knowledge from the entire vector database</p>
        </div>
      </div>
    </div>
    
    <div className="mt-6 text-center">
      <p className="text-xl font-bold bg-gradient-to-r from-green-400 to-emerald-400 bg-clip-text text-transparent">
        This is the power of AI-Native development! 🚀
      </p>
    </div>
  </div>
</div>

</div>
